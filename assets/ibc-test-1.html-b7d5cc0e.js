import{_ as e,o as a,c as s,e as n}from"./app-5df0c930.js";const r={},t=n(`<h1 id="ibc-test-1" tabindex="-1"><a class="header-anchor" href="#ibc-test-1" aria-hidden="true">#</a> IBC Test 1</h1><p>In this example, you will be testing the <code>ibc</code> module using two instances of <code>chora-chain</code>.</p><p><code>chora-chain</code> was scaffolded using <code>starport</code>, so this first example will be relatively simple as most of the configuration and setup is already done for you.</p><h2 id="run-first-chain" tabindex="-1"><a class="header-anchor" href="#run-first-chain" aria-hidden="true">#</a> Run First Chain</h2><p><em>Note: The following commands should be run from within the <code>chora-chain</code> repository.</em></p><p>Initialize and start the node for the first chain (<code>chora-1</code>).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>starport serve <span class="token parameter variable">-c</span> config/chora-chora/starport/chora-1.yml
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="run-second-chain" tabindex="-1"><a class="header-anchor" href="#run-second-chain" aria-hidden="true">#</a> Run Second Chain</h2><p><em>Note: The following commands should be run from within the <code>chora-chain</code> repository.</em></p><p>Initialize and start the node for the second chain (<code>chora-2</code>).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>starport serve <span class="token parameter variable">-c</span> config/chora-chora/starport/chora-2.yml
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="run-chain-relayer" tabindex="-1"><a class="header-anchor" href="#run-chain-relayer" aria-hidden="true">#</a> Run Chain Relayer</h2><p>Configure the <code>relayer</code> using the <code>advanced</code> flag option.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>starport relayer configure <span class="token parameter variable">--advanced</span> --source-rpc <span class="token string">&quot;http://0.0.0.0:26657&quot;</span> --source-faucet <span class="token string">&quot;http://0.0.0.0:4500&quot;</span> --source-port <span class="token string">&quot;transfer&quot;</span> --source-version <span class="token string">&quot;ics20-1&quot;</span> --target-rpc <span class="token string">&quot;http://0.0.0.0:26659&quot;</span> --target-faucet <span class="token string">&quot;http://0.0.0.0:4501&quot;</span> --target-port <span class="token string">&quot;transfer&quot;</span> --target-version <span class="token string">&quot;ics20-1&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Start the <code>relayer</code> and begin communication between chains.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>starport relayer connect
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="test-transfer" tabindex="-1"><a class="header-anchor" href="#test-transfer" aria-hidden="true">#</a> Test Transfer</h2><p>...</p>`,18),o=[t];function i(c,d){return a(),s("div",null,o)}const l=e(r,[["render",i],["__file","ibc-test-1.html.vue"]]);export{l as default};
