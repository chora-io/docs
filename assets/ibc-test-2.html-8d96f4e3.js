import{_ as e,o as a,c as s,e as n}from"./app-5df0c930.js";const r={},i=n(`<h1 id="ibc-test-2" tabindex="-1"><a class="header-anchor" href="#ibc-test-2" aria-hidden="true">#</a> IBC Test 2</h1><p>In this example, you will be testing the <code>ibc</code> module using two instances of <code>chora-chain</code>.</p><p>This example essentially leads to the same results as the previous example (&quot;IBC Test 1&quot;) but it does not use the simplified <code>starport</code> commands and configuration.</p><h2 id="run-first-chain" tabindex="-1"><a class="header-anchor" href="#run-first-chain" aria-hidden="true">#</a> Run First Chain</h2><p><em>Note: The following commands should be run from within the <code>chora-chain</code> repository, which is also being used as the <code>home</code> directory for the purpose of testing (i.e. <code>--home .chora-1</code>).</em></p><p>Using the <code>chorad</code> binary, you will need to initialize the node for the first chain (<code>chora-1</code>).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>chorad init node-1 --chain-id chora-1 <span class="token parameter variable">--home</span> .chora-1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Once the node has been initialized, you will need to create a validator key and then store the address of the validator for later use.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token assign-left variable">KEY_NAME</span><span class="token operator">=</span>chora-1-validator

chorad keys <span class="token function">add</span> <span class="token variable">$KEY_NAME</span> <span class="token parameter variable">--home</span> .chora-1

<span class="token assign-left variable">VALIDATOR_ADDRESS</span><span class="token operator">=</span><span class="token variable"><span class="token variable">$(</span>chorad keys show $KEY_NAME <span class="token parameter variable">-a</span> <span class="token parameter variable">--home</span> .chora-1<span class="token variable">)</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Now that you have generated a validator key, you will need to add the public address to the genesis file along with an initial amount of stake.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>chorad add-genesis-account <span class="token variable">$VALIDATOR_ADDRESS</span> 5000000stake <span class="token parameter variable">--home</span> .chora-1 
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Then you will need to create the genesis transaction.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>chorad gentx <span class="token variable">$KEY_NAME</span> 1000000stake --chain-id chora-1 <span class="token parameter variable">--home</span> .chora-1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Once you have created the genesis transaction, you will need to write it to the genesis file.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>chorad collect-gentxs <span class="token parameter variable">--home</span> .chora-1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now you are ready to start and run the node for the <code>chora-1</code> chain.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>chorad start <span class="token parameter variable">--home</span> .chora-1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="run-second-chain" tabindex="-1"><a class="header-anchor" href="#run-second-chain" aria-hidden="true">#</a> Run Second Chain</h2><p><em>Note: The following commands should be run from within the <code>chora-chain</code> repository, which is also being used as the <code>home</code> directory for the purpose of testing (i.e. <code>--home .chora-2</code>).</em></p><p>Using the <code>chorad</code> binary, you will need to initialize the node for the second chain (<code>chain-2</code>).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>chorad init node-2 --chain-id chora-2 <span class="token parameter variable">--home</span> .chora-2
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Once the node has been initialized, you will need to create a validator key and then store the address of the validator for later use.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token assign-left variable">KEY_NAME</span><span class="token operator">=</span>chora-2-validator

chorad keys <span class="token function">add</span> <span class="token variable">$KEY_NAME</span> <span class="token parameter variable">--home</span> .chora-2

<span class="token assign-left variable">VALIDATOR_ADDRESS</span><span class="token operator">=</span><span class="token variable"><span class="token variable">$(</span>chorad keys show $KEY_NAME <span class="token parameter variable">-a</span> <span class="token parameter variable">--home</span> .chora-2<span class="token variable">)</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Now that you have generated a validator key, you will need to add the public address to the genesis file along with an initial amount of stake.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>chorad add-genesis-account <span class="token variable">$VALIDATOR_ADDRESS</span> 5000000stake <span class="token parameter variable">--home</span> .chora-2 
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Then you will need to create the genesis transaction.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>chorad gentx <span class="token variable">$KEY_NAME</span> 1000000stake --chain-id chora-2 <span class="token parameter variable">--home</span> .chora-2
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Once you have created the genesis transaction, you will need to write it to the genesis file.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>chorad collect-gentxs <span class="token parameter variable">--home</span> .chora-2
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now you are ready to start and run the node for the <code>chora-2</code> chain. For this node, you will need to change the default ports using the appropriate flags.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>chorad start <span class="token parameter variable">--grpc.address</span> <span class="token number">0.0</span>.0.0:9191 <span class="token parameter variable">--p2p.laddr</span> tcp://127.0.0.1:26658 <span class="token parameter variable">--rpc.laddr</span> tcp://127.0.0.1:26659 <span class="token parameter variable">--rpc.pprof_laddr</span> <span class="token number">127.0</span>.0.1:6161 <span class="token parameter variable">--home</span> .chora-2
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="run-chain-relayer" tabindex="-1"><a class="header-anchor" href="#run-chain-relayer" aria-hidden="true">#</a> Run Chain Relayer</h2><p><em>Note: The following commands should be run from within the <code>chora-chain</code> repository, which is also being used as the <code>home</code> directory for the purpose of testing (i.e. <code>--home .relayer</code>).</em></p><p>Using the <code>rly</code> binary, initialize the <code>relayer</code> service.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>rly config init <span class="token parameter variable">--home</span> .relayer
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Add <code>chains</code> to the <code>relayer</code> configuration.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>rly config add-chains config/chora-chora/chains <span class="token parameter variable">--home</span> .relayer
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Add <code>paths</code> to the <code>relayer</code> configuration.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>rly config add-paths config/chora-chora/paths <span class="token parameter variable">--home</span> .relayer
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Add keys for each chain for the <code>relayer</code>. <em>Note that the same keys used for the validators are being used for the light client operators to simplify the number of steps for testing purposes.</em></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>rly keys restore chora-1 chora-1-validator <span class="token string">&quot;[mnemonic]&quot;</span> <span class="token parameter variable">--home</span> .relayer
rly keys restore chora-2 chora-2-validator <span class="token string">&quot;[mnemonic]&quot;</span> <span class="token parameter variable">--home</span> .relayer
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Initialize a light client database for each chain.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>rly light init chora-1 <span class="token parameter variable">-f</span> <span class="token parameter variable">--home</span> .relayer
rly light init chora-2 <span class="token parameter variable">-f</span> <span class="token parameter variable">--home</span> .relayer
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Link paths between chains and complete <code>relayer</code> configuration.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>rly tx <span class="token function">link</span> transfer <span class="token parameter variable">--home</span> .relayer
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Start the <code>relayer</code> service.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>rly start transfer <span class="token parameter variable">--home</span> .relayer
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="test-transfer" tabindex="-1"><a class="header-anchor" href="#test-transfer" aria-hidden="true">#</a> Test Transfer</h2><p>...</p>`,49),o=[i];function d(t,l){return a(),s("div",null,o)}const h=e(r,[["render",d],["__file","ibc-test-2.html.vue"]]);export{h as default};
