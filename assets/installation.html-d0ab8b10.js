import{_ as t,r as o,o as i,c as l,a as e,b as a,d as s,e as r}from"./app-5df0c930.js";const d={},c=e("h1",{id:"installation",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#installation","aria-hidden":"true"},"#"),a(" Installation")],-1),h=e("h2",{id:"prerequisites",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#prerequisites","aria-hidden":"true"},"#"),a(" Prerequisites")],-1),u=e("p",null,"In order to run blockchain clients built with the Cosmos SDK, you will need the following:",-1),p={href:"https://golang.org/doc/install",target:"_blank",rel:"noopener noreferrer"},g={href:"https://github.com/tendermint/starport",target:"_blank",rel:"noopener noreferrer"},m=e("h2",{id:"install-chora",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#install-chora","aria-hidden":"true"},"#"),a(" Install Chora")],-1),b={href:"https://github.com/chora-io/chora-chain",target:"_blank",rel:"noopener noreferrer"},v=r(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> clone https://github.com/chora-io/chora-chain
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>After you have cloned the remote repository, change to the local directory.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">cd</span> chora-chain
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>From the local directory, run the following command to install the <code>chorad</code> binary.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">make</span> <span class="token function">install</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now that the <code>chorad</code> binary is installed, you are ready to start and run <code>chora-chain</code>.</p><h2 id="install-regen" tabindex="-1"><a class="header-anchor" href="#install-regen" aria-hidden="true">#</a> Install Regen</h2>`,7),f={href:"https://github.com/regen-network/regen-ledger",target:"_blank",rel:"noopener noreferrer"},_=r(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> clone https://github.com/regen-network/regen-ledger
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>After you have cloned the remote repository, change to the local directory.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">cd</span> regen-ledger
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>From the local directory, switch to the latest release version (<code>v1.0.0</code>).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> checkout v1.0.0
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>After switching versions, run the following command to install the <code>regen</code> binary. <em>Note the use of the <code>EXPERIMENTAL=true</code> option here, which enables the custom modules built within <code>regen-ledger</code>.</em></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token assign-left variable">EXPERIMENTAL</span><span class="token operator">=</span>true <span class="token function">make</span> <span class="token function">install</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now that the <code>regen</code> binary is installed, you are ready to start and run <code>regen-ledger</code>.</p><h2 id="install-relayer" tabindex="-1"><a class="header-anchor" href="#install-relayer" aria-hidden="true">#</a> Install Relayer</h2>`,9),y={href:"https://github.com/cosmos/relayer",target:"_blank",rel:"noopener noreferrer"},k=r(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> clone https://github.com/cosmos/relayer
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>After you have cloned the remote repository, change to the local directory.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">cd</span> relayer
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>From the local directory, switch to the latest release version (<code>v0.9.2</code>).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> checkout v0.9.2
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>After switching versions, run the following command to install the <code>rly</code> binary.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">make</span> <span class="token function">install</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now that the <code>rly</code> binary is installed, you are ready to start and run <code>relayer</code>.</p>`,8);function x(w,N){const n=o("ExternalLinkIcon");return i(),l("div",null,[c,h,u,e("ul",null,[e("li",null,[e("a",p,[a("Go >= 1.15"),s(n)])]),e("li",null,[e("a",g,[a("Starport >= 0.15"),s(n)])])]),m,e("p",null,[a("From a working directory, clone the remote repository for "),e("a",b,[a("chora-chain"),s(n)]),a(".")]),v,e("p",null,[a("From a working directory, clone the remote repository for "),e("a",f,[a("regen-ledger"),s(n)]),a(".")]),_,e("p",null,[a("From a working directory, clone the remote repository for "),e("a",y,[a("relayer"),s(n)]),a(".")]),k])}const I=t(d,[["render",x],["__file","installation.html.vue"]]);export{I as default};
